# Railway Configuration - Mubyn OS Backend
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "NODE_ENV=production node scripts/dashboard-server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
